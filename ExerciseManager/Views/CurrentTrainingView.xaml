<UserControl x:Class="ExerciseManager.Views.CurrentTrainingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExerciseManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Left" Width="300">
        <TextBlock Text="{Binding ErrorMessage}" FontSize="20" FontWeight="SemiBold" TextAlignment="Center"/>
        <!--<ListBox ItemsSource = "{Binding ExerciseSetsList}">
        </ListBox>-->
        <TreeView x:Name="tv_ExerciseSetListTree" MouseDoubleClick="OnDoubleClick" ItemsSource="{Binding ExerciseSetsTreeView}"/>

    </StackPanel>
</UserControl>
