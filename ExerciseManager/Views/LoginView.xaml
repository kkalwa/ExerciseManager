<UserControl x:Class="ExerciseManager.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExerciseManager.Views"
             xmlns:customControls="clr-namespace:ExerciseManager.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="Peru"  Offset="0"/>
                <GradientStop Color="Orange" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.Resources>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#FFFBE33E"  Offset="0"/>
                            <GradientStop Color="#FFE6DE29" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.ColumnSpan="2" Content="Zaloguj się" FontSize="40" HorizontalAlignment="Center">
            
        </Label>


        <StackPanel Grid.Column="0" Grid.Row="1">
            <Label HorizontalContentAlignment="Right" Content="Podaj nazwę użytkownika:"/>
            <Label HorizontalContentAlignment="Right" Content="Podaj haslo:"/>

        </StackPanel>

        <Image Grid.Column="0" Grid.Row="1" 
               Opacity="1" 
               HorizontalAlignment="Center"
                VerticalAlignment="Center"
               Source="/Images/Logo_NoBackground.png"/>

        <StackPanel Grid.Column="1" Grid.Row="1">
            <TextBox Margin="5" Text="{Binding Login}"/>
            <TextBox Margin="5" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
            <!--<customControls:CustomPasswordBox Margin="5" Password="{Binding Password, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>-->
            <Button Margin="5" Height="20" Content="Zaloguj" Command="{Binding InitiateLogInProcessCommand}"/>
            <Label HorizontalAlignment="Left" Content="{Binding LoginStatus}"/>
            <Label  Content="...lub utwórz nowy profil" HorizontalAlignment="Center"/>
            <Button  Height="20" Width="100" Content="Stwórz profil" HorizontalAlignment="Center"
                Command="{Binding CreateProfileCommand}">
            </Button>
            
        </StackPanel>
        
    </Grid>
</UserControl>
