<UserControl x:Class="ExerciseManager.Views.UserPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:ExerciseManager.ViewModels"
             
             xmlns:local="clr-namespace:ExerciseManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="Peru"  Offset="0"/>
                <GradientStop Color="Orange" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" Grid.Row="0" Margin="5" FontSize="20" Text="Witaj w panelu użytkownika" TextAlignment="Right"/>
        <Button Grid.Column="2" Grid.Row="0" Margin="5" Content="Wyloguj się" Padding="5" Command="{Binding LogOutCommand}"/>
       <!-- <UserControl Grid.Column="1" Grid.Row="1" Margin="5" Content="{Binding CurrentUserManageExercises, Mode=TwoWay}"/>
        <UserControl Grid.Column="1" Grid.Row="1" Margin="5" Content="{Binding CurrentUserHistory}"/>-->
        <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding CurrentUserControl}" >
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type viewmodels:ManageExercisesViewModel}">
                    <local:ManageExercisesView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:HistoryViewModel}">
                    <local:HistoryView/>
                </DataTemplate>
                
            </ContentControl.Resources>
        </ContentControl>
        <StackPanel Grid.Column="0" Grid.Row="1">
            <Button Content="Zarządzaj ćwiczeniami" Margin="5" Command="{Binding ManageExercisesCommand}"/>
            <Button Content="Historia" Margin="5" Command="{Binding CheckHistoryCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
