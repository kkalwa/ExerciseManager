<UserControl x:Class="ExerciseManager.Views.ManageExercisesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExerciseManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<WrapPanel x:Name="container" Orientation="horizontal" Height="100" VerticalAlignment="Top">
        <Button Content="Add Exercise" Margin="5" Padding="10,5" Click="btnNewElement"/>
        
    </WrapPanel>-->
    
    <ItemsControl  ItemsSource="{Binding ListOfUserSets}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <WrapPanel
                    Background="AliceBlue">
                    <Button Content="{Binding ExerciseSetTitle}"></Button>
                    <Separator/>
                </WrapPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

    <!--<ListBox ItemsSource="{Binding ListOfUserSets}">
        <Button Content="{Binding ExerciseSetTitle}"/>
    </ListBox>-->
    
</UserControl>
